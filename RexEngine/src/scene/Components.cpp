#include "REPch.h"
#include "Components.h"

//template<typename Archive>
//void serialize(Archive& archive, RexEngine::MeshRendererComponent& c) {
//	archive(c.cullingMode, c.priority); // TODO : add shader and mesh assets
//}

//template<typename Archive>
//void serialize(Archive& archive, RexEngine::TransformComponent& c) {
//	archive(c.position, c.scale, c.rotation, c.parent);
//}
//
//template<typename Archive>
//void serialize(Archive& archive, RexEngine::CameraComponent& c) {
//	archive(c.fov, c.zNear, c.zFar);
//}
//
//template<typename Archive>
//void serialize(Archive& archive, RexEngine::SkyboxComponent& c) {
//	//archive(c.fov, c.zNear, c.zFar); // TODO : shader asset
//}